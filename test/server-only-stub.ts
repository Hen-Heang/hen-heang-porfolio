// Stub for the `server-only` package used only by the Vitest config alias —
// tests run in a server-equivalent Node context, so the real package's
// unconditional throw (meant to catch client-bundle imports) doesn't apply.
export {}
