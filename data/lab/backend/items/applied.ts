import type { BackendInterviewItem, BackendLabItem, BackendNarrativeItem } from "@/src/lib/types/backend-engineering"
import { createNarrativeSections } from "@/data/lab/backend/helpers"
import { backendSources as s } from "@/data/lab/backend/sources"

const updatedAt = "2026-07-16"

export const backendTaskApiLab: BackendLabItem = {
    id: "backend-spring-boot-task-api-lab",
    slug: "spring-boot-task-api-lab",
    title: "Lab: Build a Spring Boot Task API",
    description: "Build a Task API from Spring Initializr and PostgreSQL schema through MyBatis, validation, errors, transactions, tests, Docker, CI, deployment, and operations.",
    level: 4,
    category: "spring",
    type: "lab",
    technologies: ["Java 21", "Spring Boot 3.5", "Spring MVC", "MyBatis", "PostgreSQL", "Docker"],
    keywords: ["beginner lab", "Task API", "validation", "Testcontainers", "Docker", "deployment"],
    prerequisiteIds: ["backend-spring-boot-layered-architecture", "backend-postgresql-schema-indexing-fundamentals"],
    relatedIds: ["backend-testing-strategy", "backend-docker-cicd-spring-boot", "backend-production-readiness-checklist"],
    learningObjectives: ["Build one complete backend vertical slice with intentional layer boundaries", "Carry the same feature through data correctness, tests, packaging, delivery, and operations"],
    difficulty: "beginner",
    estimatedMinutes: 480,
    featured: true,
    status: "published",
    updatedAt,
    versionScope: "Java 21, Spring Boot 3.5.16, MyBatis starter 3.x, PostgreSQL 16+",
    sources: [s.springBoot35, s.springMvc, s.mybatisStarter, s.postgresql, s.springTesting, s.docker],
    overview: [
        {
            id: "outcome",
            title: "Outcome",
            blocks: [
                { type: "paragraph", text: "You will build a credential-free local Task API with create, read, keyset list, complete, and delete behavior. The final artifact has validation, RFC 9457 errors, ownership, transactions, PostgreSQL integration tests, a non-root container, health checks, CI gates, and a deployment/rollback runbook." },
                { type: "diagram", title: "Lab architecture", steps: ["HTTP client", "Spring MVC", "Task service", "MyBatis mapper", "PostgreSQL", "Actuator + logs"], textAlternative: "An HTTP client calls Spring MVC. A Task service owns business rules and transactions, a MyBatis mapper accesses PostgreSQL, and Actuator plus structured logs expose operational signals." },
            ],
        },
        {
            id: "requirements",
            title: "Product and API requirements",
            blocks: [
                { type: "list", items: ["An authenticated user can create, read, list, complete, and delete only their tasks", "Title is required and at most 160 characters; dueAt is an optional instant", "Completion is allowed only from OPEN and is idempotent at the state level", "List order is createdAt DESC, id DESC with opaque keyset cursor", "Every expected failure has a stable machine code and traceId", "No test or local build requires production credentials"] },
                { type: "table", headers: ["Endpoint", "Behavior"], rows: [["POST /v1/tasks", "Create; 201 + Location"], ["GET /v1/tasks/{id}", "Owner-scoped read"], ["GET /v1/tasks?cursor=&limit=", "Stable keyset page"], ["POST /v1/tasks/{id}/completion", "Conditional OPEN → DONE transition"], ["DELETE /v1/tasks/{id}", "Owner-scoped deletion policy"]] },
            ],
        },
        {
            id: "definition-of-done",
            title: "Definition of done",
            blocks: [
                { type: "list", items: ["Schema constraints are the final correctness boundary", "Controllers contain no business or SQL logic", "Mapper SQL uses bound parameters and explicit columns", "Rollback, authorization, keyset order, and concurrent completion are tested", "Image runs as a non-root user and exits gracefully on SIGTERM", "A fresh environment can deploy, smoke-test, observe, and roll back the documented artifact"] },
                { type: "callout", tone: "production", title: "Learning rule", text: "Commit after each milestone with a meaningful message. Keep a decision log for every shortcut, trade-off, and production follow-up so the lab demonstrates reasoning as well as code." },
            ],
        },
    ],
    milestones: [
        {
            id: "bootstrap",
            title: "1. Bootstrap and configuration",
            goal: "Create a reproducible Java 21 Spring Boot application with explicit typed configuration.",
            tasks: ["Generate Maven project with Web, Validation, Security, Actuator, PostgreSQL, MyBatis, and test dependencies", "Add feature-based packages", "Bind application properties for page limits and security settings", "Create local/test/prod profiles without secrets"],
            acceptanceCriteria: ["Application starts with Java 21", "Invalid required configuration fails startup with a useful message", "Repository contains no credential"],
            tests: ["Context smoke test with test configuration", "Configuration binding validation test"],
            commonRisks: ["Copying dependency versions that conflict with the Spring Boot BOM", "Committing local database passwords"],
            productionNotes: ["Boot 3.5 requires Java 17+; this lab standardizes on Java 21", "MyBatis starter 3.x supports Boot 3.2–3.5"],
        },
        {
            id: "schema-migration",
            title: "2. Schema and migration",
            goal: "Make PostgreSQL protect task identity, ownership, valid status, title, and stable list access.",
            tasks: ["Create app_user and task migrations", "Add named primary, foreign, check, and not-null constraints", "Add owner/status/created/id list index", "Seed only test/local identities"],
            acceptanceCriteria: ["Invalid title, status, or owner fails at the database", "Keyset list query can use the intended index", "Migration applies to an empty database"],
            tests: ["Migration integration test", "Constraint failure tests", "Representative EXPLAIN review"],
            commonRisks: ["Application-only enum validation", "Index order that does not match predicates and ordering"],
            productionNotes: ["Use a migration tool in the implementation; run migrations as a controlled deployment job", "Design future changes with expand/contract compatibility"],
        },
        {
            id: "vertical-slice",
            title: "3. Controller, service, and MyBatis vertical slice",
            goal: "Implement create and owner-scoped read through every layer.",
            tasks: ["Define request/response records", "Implement thin controller", "Implement service use cases and mapping", "Create typed mapper interface, resultMap, insert, and select SQL"],
            acceptanceCriteria: ["POST returns 201 and Location", "GET returns the expected owner-scoped task", "SQL has explicit columns and bound parameters"],
            tests: ["Service unit tests", "MockMvc create/read contract tests", "PostgreSQL mapper integration tests"],
            commonRisks: ["Returning TaskRow as the API response", "Using raw substitution in mapper XML"],
            productionNotes: ["Interpret affected rows and missing ownership deliberately to avoid information disclosure"],
        },
        {
            id: "validation-errors",
            title: "4. Validation and error contract",
            goal: "Create predictable syntax, business, conflict, not-found, and infrastructure failure behavior.",
            tasks: ["Add Jakarta Bean Validation", "Add business-state checks", "Implement @RestControllerAdvice with ProblemDetail", "Add stable codes, field errors, instance, and traceId"],
            acceptanceCriteria: ["Malformed input is 400", "Unknown or inaccessible task follows the chosen 404 policy", "Unexpected failures do not leak stack or SQL details"],
            tests: ["Validation matrix", "Problem-details JSON contract test", "Unexpected exception redaction test"],
            commonRisks: ["Duplicating handlers per controller", "Logging the same exception at every layer"],
            productionNotes: ["RFC 9457 obsoletes RFC 7807; keep extension fields stable and documented"],
        },
        {
            id: "transactions-concurrency",
            title: "5. Transactions, list, and concurrency",
            goal: "Implement stable pagination and an atomic OPEN-to-DONE transition.",
            tasks: ["Add keyset list query and opaque cursor codec", "Add transactional completion use case", "Use conditional UPDATE with owner and expected status", "Choose repeated-completion response semantics"],
            acceptanceCriteria: ["Pages contain no duplicates with equal timestamps", "Concurrent completion never creates invalid state", "Rollback leaves no partial audit row"],
            tests: ["Keyset pagination integration test", "Rollback test", "Bounded concurrent completion test"],
            commonRisks: ["Offset paging under concurrent inserts", "Read-then-write race without conditional SQL"],
            productionNotes: ["Keep the transaction short and do not send notifications inside it"],
        },
        {
            id: "security",
            title: "6. Security and ownership",
            goal: "Authenticate test users and enforce authorization at request, method, and query boundaries.",
            tasks: ["Configure SecurityFilterChain", "Choose session authentication for the lab browser client", "Enable CSRF for unsafe cookie-authenticated requests", "Resolve actor identity without trusting request ownerId"],
            acceptanceCriteria: ["Anonymous requests are 401", "Wrong owner cannot read or mutate a task", "CSRF-less unsafe browser request is rejected", "Admin behavior is explicitly scoped"],
            tests: ["Spring Security MockMvc matrix", "Horizontal-access integration test", "Session fixation/logout tests"],
            commonRisks: ["Accepting ownerId from request body", "Disabling CSRF because responses are JSON"],
            productionNotes: ["A real deployment needs account lifecycle, adaptive password hashes, abuse controls, and secret rotation"],
        },
        {
            id: "container-ci",
            title: "7. Container and CI",
            goal: "Create one reproducible non-root artifact that passes automated quality gates.",
            tasks: ["Add multi-stage Dockerfile and .dockerignore", "Run as explicit non-root UID", "Add compile, unit, integration, build, and scan gates", "Document full-SHA pinning for third-party actions"],
            acceptanceCriteria: ["Image contains the runtime artifact, not source or build cache", "Container process is non-root", "CI needs no production secret", "Failed tests prevent image promotion"],
            tests: ["Container starts and serves readiness", "Image user inspection", "CI workflow validation"],
            commonRisks: ["Mutable latest image identity", "Broad workflow permissions", "Docker health check replacing orchestrator readiness design"],
            productionNotes: ["Promote by digest and automate reviewed base-image updates"],
        },
        {
            id: "deploy-operate",
            title: "8. Deploy, observe, and recover",
            goal: "Deploy with safe migration, health, smoke, logging, metrics, alerts, graceful shutdown, and rollback evidence.",
            tasks: ["Configure Nginx or platform routing and HTTPS", "Secure Actuator and separate readiness/liveness", "Add structured correlation logs and core metrics", "Write deployment, smoke, database-pool incident, and rollback runbooks"],
            acceptanceCriteria: ["Only ready instances receive traffic", "SIGTERM drains accepted work", "Smoke test verifies create/read/list/complete", "Previous image can be restored without schema incompatibility"],
            tests: ["Post-deploy smoke test", "Probe failure simulation", "Pool-exhaustion drill", "Rollback exercise"],
            commonRisks: ["Exposing all Actuator endpoints publicly", "Alerting without an owner or action", "Assuming backup exists without restore evidence"],
            productionNotes: ["Record dashboard, alert, restore, and rollback evidence in the project README or runbook"],
        },
    ],
}

export const backendInterviewCollection: BackendInterviewItem = {
    id: "backend-interview-questions",
    slug: "backend-interview-questions",
    title: "Java and Spring Backend Interview Questions",
    description: "Practice production-oriented Java, HTTP, PostgreSQL, Spring, MyBatis, security, testing, reliability, DevOps, and system-design discussions.",
    level: 7,
    category: "testing",
    type: "interview",
    technologies: ["Java", "Spring Boot", "MyBatis", "PostgreSQL", "Docker"],
    keywords: ["interview", "Java questions", "Spring questions", "database questions", "system design"],
    prerequisiteIds: ["backend-java-backend-fundamentals", "backend-spring-transaction-fundamentals"],
    relatedIds: ["backend-voucher-redemption-case-study", "backend-production-readiness-checklist", "backend-testing-strategy"],
    learningObjectives: ["Structure backend interview answers around mechanism, trade-off, evidence, and failure modes", "Connect language and framework facts to real production decisions"],
    difficulty: "intermediate",
    estimatedMinutes: 90,
    featured: true,
    status: "published",
    updatedAt,
    versionScope: "Java 21 baseline, Spring Boot 3.5 / Security 6.5 / MyBatis starter 3.x",
    sources: [s.java25, s.springBoot35, s.springTransactions, s.springSecurity, s.postgresql, s.docker],
    introduction: "Answer each question in four moves: define the mechanism, apply it to a concrete backend flow, state the trade-off or failure mode, and name the evidence or test you would use. Avoid memorized absolutes such as “JWT is stateless and therefore better.”",
    questions: [
        { question: "How do equals and hashCode affect a HashMap used by backend code?", focus: "Java value semantics and collection correctness", strongAnswer: ["Equal objects must have equal hash codes; keys should not mutate in fields used for equality while stored", "Explain collision lookup and connect the contract to DTO/value-object design"], weakSignals: ["Says HashMap compares only hash codes"], followUps: ["What changes with a record key?", "When would ConcurrentHashMap help—and not help?"] },
        { question: "What creates a Java memory leak when the JVM has garbage collection?", focus: "Reachability, retention, and diagnostic evidence", strongAnswer: ["Objects remain reachable through unintended long-lived roots such as static caches, listeners, ThreadLocal values, or unbounded queues", "Use heap dump, dominator/retained-size analysis, allocation evidence, and lifecycle review"], weakSignals: ["Says GC prevents all memory leaks"], followUps: ["How can ThreadLocal interact with request pools or virtual threads?"] },
        { question: "What happens during an HTTPS request to a Spring Boot API?", focus: "End-to-end request lifecycle", strongAnswer: ["Cover DNS, TCP, TLS, proxy/load balancer, servlet filters/Security, MVC mapping, service, transaction, SQL, serialization, and response", "Name timeout, logging, and trace boundaries"], weakSignals: ["Starts and ends at the controller"], followUps: ["Where can 502, 503, and 504 originate?"] },
        { question: "How would you design an idempotent payment or voucher-redemption endpoint?", focus: "HTTP, transactions, uniqueness, and concurrent duplicates", strongAnswer: ["Scope an idempotency key, bind it to a request fingerprint, atomically persist outcome with a unique constraint, and return the stored result for an exact replay", "Discuss in-progress state, expiry, retries, and changed-payload conflict"], weakSignals: ["Only checks an in-memory cache"], followUps: ["What if the client times out after commit?"] },
        { question: "How does a composite PostgreSQL B-tree index support a query?", focus: "Predicate and ordering alignment", strongAnswer: ["Start from equality predicates, then range/order columns and a unique tie-breaker; leading columns determine useful scan bounds", "Verify estimated/actual rows, scan, buffers, sort, and write cost with representative plans"], weakSignals: ["Adds every filtered column in arbitrary order"], followUps: ["When is a sequential scan the right plan?"] },
        { question: "How does @Transactional work, and why can self-invocation fail?", focus: "Spring AOP proxy semantics and rollback", strongAnswer: ["An external call enters a Spring proxy that uses transaction metadata and a transaction manager around the method", "A same-instance call bypasses the default proxy; RuntimeException/Error roll back by default while checked exceptions do not"], weakSignals: ["Treats the annotation as compiler magic"], followUps: ["Why avoid remote calls inside the boundary?", "What does REQUIRES_NEW cost?"] },
        { question: "How do #{} and ${} differ in MyBatis?", focus: "SQL binding and injection", strongAnswer: ["#{} binds a prepared-statement parameter; ${} inserts raw text", "Dynamic identifiers need a server-owned allowlist mapped to fixed fragments"], weakSignals: ["Says both are safe placeholders"], followUps: ["How would you test dynamic sorting?"] },
        { question: "When would you choose a server session instead of JWT?", focus: "Authentication trade-offs", strongAnswer: ["A single browser application often benefits from compact secure cookies and straightforward server invalidation", "Discuss CSRF for automatic cookies, shared session storage, token revocation, XSS, independent resource servers, and logout"], weakSignals: ["Calls JWT automatically more secure or truly stateless in every system"], followUps: ["How does refresh-token rotation detect reuse?"] },
        { question: "What belongs in unit, MVC slice, and PostgreSQL integration tests?", focus: "Risk-based test boundaries", strongAnswer: ["Unit tests cover pure rules and service decisions; MVC slices cover mapping/validation/security; real PostgreSQL covers SQL, constraints, result maps, rollback, and concurrency", "Use a small full-flow set for wiring and deployment"], weakSignals: ["Mocks PostgreSQL behavior to verify SQL"], followUps: ["How do you keep concurrent tests deterministic?"] },
        { question: "How would you diagnose database connection-pool exhaustion?", focus: "Observability and causal investigation", strongAnswer: ["Correlate latency/errors with active, idle, pending, acquisition time, database sessions, slow queries, locks, transaction duration, and thread state", "Mitigate load/leaks/slow work first; do not blindly increase pool size beyond database capacity"], weakSignals: ["Only restarts the service or doubles the pool"], followUps: ["What alert and runbook improvement follows?"] },
        { question: "When should a small system use a modular monolith rather than microservices?", focus: "System boundary and operational cost", strongAnswer: ["Prefer one deployment with enforced module boundaries until independent ownership, scaling, reliability, or release evidence justifies distribution", "Compare network failure, data consistency, observability, delivery, and team costs"], weakSignals: ["Chooses microservices because they scale by definition"], followUps: ["How would you prepare a module for a later split?"] },
        { question: "What makes a Spring Boot deployment production ready?", focus: "Delivery and operations evidence", strongAnswer: ["Name immutable artifact, non-root image, secrets, migration, probes, graceful shutdown, limits, tests/scans, logs/metrics/traces, alerts, backups/restores, smoke test, rollback, and ownership", "Distinguish readiness, liveness, and business smoke signals"], weakSignals: ["Says Docker and HTTPS are sufficient"], followUps: ["How do you roll back after a schema change?"] },
    ],
}

export const backendVoucherCaseStudy: BackendNarrativeItem = {
    id: "backend-voucher-redemption-case-study",
    slug: "voucher-redemption-production-case-study",
    title: "Production Case Study: B2B Voucher Redemption",
    description: "Design a fictional modular-monolith voucher platform with atomic redemption, idempotency, audit history, settlement outbox, security, testing, delivery, and operations.",
    level: 12,
    category: "capstone",
    type: "case-study",
    technologies: ["Next.js", "Java 21", "Spring Boot", "MyBatis", "PostgreSQL", "Redis", "Docker"],
    keywords: ["voucher", "fintech", "modular monolith", "idempotency", "outbox", "settlement"],
    prerequisiteIds: ["backend-production-readiness-checklist", "backend-docker-cicd-spring-boot"],
    relatedIds: ["backend-spring-transaction-fundamentals", "backend-spring-security-authentication-flow", "backend-testing-strategy"],
    learningObjectives: ["Design an atomic and idempotent voucher redemption boundary", "Connect architecture decisions to tests, deployment, monitoring, incidents, and rollback"],
    difficulty: "advanced",
    estimatedMinutes: 90,
    featured: true,
    status: "published",
    updatedAt,
    versionScope: "Fictional reference architecture using Java 21 and Spring Boot 3.5; no employer-confidential system details",
    sources: [s.springBoot35, s.postgresqlIsolation, s.mybatisStarter, s.springSecurity, s.owaspAsvs, s.outbox, s.opentelemetry],
    sections: createNarrativeSections({
        whatItIs: "This fictional B2B Voucher and Settlement Platform lets authorized issuers create voucher batches, merchants validate and redeem vouchers, operations users review exceptions, and finance users export settlement reports. It is a learning architecture, not a copy of any employer system.",
        whyItMatters: "Voucher redemption concentrates backend concerns: a code is sensitive, concurrent requests must not redeem twice, clients retry after timeouts, authorization spans tenant and merchant ownership, settlement is asynchronous, audit history matters, and operators need safe recovery.",
        howItWorks: [
            { type: "diagram", title: "Default modular-monolith architecture", steps: ["Next.js client", "Nginx / platform edge", "Spring Boot API", "Identity + Voucher + Redemption + Settlement modules", "MyBatis", "PostgreSQL", "Outbox worker", "Settlement provider"], textAlternative: "A Next.js client reaches the Spring Boot API through an edge proxy. Inside one deployable, identity, voucher, redemption, and settlement modules have explicit boundaries. MyBatis persists PostgreSQL state. An outbox worker later calls the fictional settlement provider." },
            { type: "callout", tone: "tradeoff", title: "Why not microservices first?", text: "One team and one transactional core benefit from one deployment and one PostgreSQL transaction boundary. Enforce module APIs and separate tables/ownership now; split only when independent scale, reliability, ownership, or release evidence exceeds distribution cost." },
        ],
        simpleExample: [
            { type: "table", headers: ["Actor", "Allowed capability"], rows: [["Issuer operator", "Create and activate batches for own tenant"], ["Merchant cashier", "Validate and redeem for assigned merchant"], ["Operations reviewer", "Investigate exceptions with reason and audit trail"], ["Finance analyst", "Read settlement reports; cannot redeem"], ["Platform administrator", "Tenant administration with separately audited privilege"]] },
            { type: "paragraph", text: "The API never accepts tenant or merchant authority merely because the client sends an ID. It derives actor scope from authenticated server-side identity and enforces ownership in service policy and SQL predicates." },
        ],
        backendExample: [
            { type: "code", language: "sql", filename: "redemption-core.sql", code: `CREATE TABLE voucher (
    id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    tenant_id bigint NOT NULL,
    code_hash bytea NOT NULL,
    status varchar(20) NOT NULL CHECK (status IN ('ISSUED','REDEEMED','EXPIRED','CANCELLED')),
    expires_at timestamptz NOT NULL,
    redeemed_at timestamptz,
    version integer NOT NULL DEFAULT 0,
    CONSTRAINT voucher_tenant_code_uk UNIQUE (tenant_id, code_hash)
);

CREATE TABLE redemption (
    id bigint GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    tenant_id bigint NOT NULL,
    merchant_id bigint NOT NULL,
    voucher_id bigint NOT NULL REFERENCES voucher(id),
    idempotency_key varchar(128) NOT NULL,
    request_hash bytea NOT NULL,
    result_code varchar(40) NOT NULL,
    created_at timestamptz NOT NULL DEFAULT now(),
    CONSTRAINT redemption_idempotency_uk
        UNIQUE (tenant_id, merchant_id, idempotency_key)
);` },
            { type: "code", language: "sql", filename: "redeem-voucher.sql", code: `UPDATE voucher
SET status = 'REDEEMED',
    redeemed_at = :now,
    version = version + 1
WHERE id = :voucherId
  AND tenant_id = :tenantId
  AND status = 'ISSUED'
  AND expires_at > :now;` },
            { type: "paragraph", text: "One affected row means the state transition won. Zero means the voucher is absent, out of scope, expired, or already terminal; the disclosure policy determines the external response. The idempotency unique key resolves concurrent replay races." },
        ],
        productionExample: [
            { type: "steps", items: [{ title: "Request", text: "Authenticate cashier, derive tenant/merchant scope, normalize and hash the voucher code, validate request, and compute a request fingerprint." }, { title: "Transaction", text: "Read any existing idempotency result, conditionally update the voucher, insert redemption and audit rows, and insert a settlement outbox event in one short transaction." }, { title: "Response", text: "Return the stored result for an exact replay. Reject reuse of the same key with a different request fingerprint." }, { title: "Async settlement", text: "A worker claims outbox records, calls a fictional provider with its own idempotency key and deadline, records attempt outcome, and retries bounded transient failures." }, { title: "Observe", text: "Measure redemption success/conflict/error rates, latency, database pool/lock state, outbox age, settlement failures, and security-denial patterns without logging raw voucher codes." }] },
            { type: "table", headers: ["Failure", "Expected behavior"], rows: [["Client timeout after commit", "Replay returns stored redemption result"], ["Two cashiers race", "Conditional update/unique constraint permits one business redemption"], ["Settlement provider down", "Redemption stays committed; outbox retry remains visible"], ["Worker crashes after provider success", "Provider idempotency and local attempt reconciliation prevent duplicate financial effect"], ["Database pool exhausted", "Readiness/load shedding and alerts protect recovery; no blind retry storm"]] },
        ],
        commonMistakes: ["Storing or logging raw voucher codes without classification", "Checking status and then updating in separate unprotected steps", "Using Redis as the correctness boundary for redemption", "Calling settlement inside the redemption transaction", "Equating one message delivery with one business effect", "Starting with microservices despite one team and one atomic core", "Omitting restore and reconciliation procedures"],
        bestPractices: ["Hash sensitive lookup codes with an appropriate keyed design and document threat assumptions", "Use constraints and conditional SQL as concurrency arbiters", "Scope idempotency to actor and operation and bind it to the request", "Keep immutable audit events separate from mutable operational status", "Use outbox plus idempotent downstream behavior", "Adopt expand/contract migrations", "Run reconciliation and incident drills"],
        tradeOffs: [{ type: "table", headers: ["Decision", "Why chosen", "Revisit when"], rows: [["Modular monolith", "One team, shared transaction, lower operational cost", "Modules need independent ownership, scale, or isolation"], ["PostgreSQL is source of truth", "Constraints and transaction semantics", "Never replace correctness with cache"], ["Redis optional", "Rate limits or short-lived non-authoritative cache", "Only with measured need and fallback"], ["Outbox", "Atomic business state and publish intent", "Operate relay, duplicates, and retention"], ["Next.js client", "Typed user/admin interface", "Backend contract remains client-independent"]] }],
        interviewQuestions: ["Prove that two concurrent redemptions cannot both succeed", "What does the idempotency request hash protect?", "Why is settlement outside the transaction?", "How do you handle a worker crash after provider success?", "Which data is safe to log?", "When would you split the settlement module?", "How do you reconcile provider and local state?"],
        handsOnTask: [
            { type: "steps", items: [
                { title: "Milestone 1 — requirements and threat model", text: "Goal: define actors, flows, abuse, privacy, and non-goals. Acceptance: reviewed API/state diagrams and threat controls. Tests: misuse cases. Risks: copying confidential behavior. Production: name owners and audit scope." },
                { title: "Milestone 2 — schema and migrations", text: "Goal: encode lifecycle, tenant scope, idempotency, audit, and outbox constraints. Acceptance: upgrade from empty and previous schema. Tests: constraint, plan, migration, and restore. Risks: incompatible enum/index change. Production: expand/contract and backup evidence." },
                { title: "Milestone 3 — identity and authorization", text: "Goal: authenticate users and enforce role, tenant, merchant, and ownership policy. Acceptance: no client-controlled authority. Tests: 401, 403, horizontal access, CSRF/token cases. Risks: privilege leakage. Production: audit and revocation." },
                { title: "Milestone 4 — atomic redemption", text: "Goal: one durable outcome per voucher and idempotent request. Acceptance: replay is stable and concurrent races create one effect. Tests: rollback, changed-payload conflict, and bounded concurrency. Risks: read/write race. Production: short transaction and explicit errors." },
                { title: "Milestone 5 — settlement outbox", text: "Goal: reliable asynchronous delivery without holding database locks. Acceptance: visible retry and reconciliation state. Tests: duplicate delivery, provider timeout, crash-after-success. Risks: unbounded retry. Production: dead-letter/runbook and outbox-age alert." },
                { title: "Milestone 6 — administration and reporting", text: "Goal: safe search, pagination, status operations, and export. Acceptance: scoped query plans and immutable audit access. Tests: authorization, keyset order, large export. Risks: data leakage. Production: retention and asynchronous export limits." },
                { title: "Milestone 7 — quality and delivery", text: "Goal: unit/integration/security tests and immutable non-root delivery. Acceptance: CI gates and smoke-tested digest. Tests: image, workflow, deployment, graceful shutdown. Risks: migration/app mismatch. Production: approval and rollback rehearsal." },
                { title: "Milestone 8 — operations", text: "Goal: logs, metrics, traces, dashboards, alerts, backup/restore, and incidents. Acceptance: pool-exhaustion and provider-outage drills. Tests: probes, alerts, restore, reconciliation, rollback. Risks: sensitive telemetry and alert fatigue. Production: runbook owners and postmortem actions." },
            ] },
        ],
        relatedTopics: ["Spring Transactions", "PostgreSQL Isolation", "Idempotency", "Transactional Outbox", "Spring Security", "Testcontainers", "Docker CI/CD", "OpenTelemetry"],
        sources: [s.springBoot35, s.postgresqlIsolation, s.mybatisStarter, s.springSecurity, s.owaspAsvs, s.outbox, s.opentelemetry],
    }),
}
